name: CI (game_kit)

# 共通パッケージ自体のCI。ここが壊れると依存する全ゲームに影響するため、
# packages/game_kit/ の変更時に単独で検証する。
on:
  push:
    branches: [master, main]
    paths:
      - 'packages/game_kit/**'
  pull_request:
    paths:
      - 'packages/game_kit/**'
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/flutter-app-ci.yml
    with:
      app-path: packages/game_kit
